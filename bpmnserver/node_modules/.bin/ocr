#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../baidu-ocr-api/bin/ocr" "$@"
  ret=$?
else 
  node  "$basedir/../baidu-ocr-api/bin/ocr" "$@"
  ret=$?
fi
exit $ret
